import { REGION_KEYWORDS } from '../generalInfo';
import { WebsiteSection } from '../types';

export const foxChattanoogaUrl = 'https://foxchattanooga.com';

const breakingLocal: WebsiteSection = {
  link: 'news/local',
  keywords: REGION_KEYWORDS,
};

export const foxChattanoogaSections = [breakingLocal];
