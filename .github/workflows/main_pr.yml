name: Run myChattanooga Hub tests

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14]

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install Dependencies
      working-directory: hub
      run: npm install

    - name: Run Unit Tests
      working-directory: hub
      run: npm run test

    - name: Run E2E Tests
      working-directory: hub
      run: npm run test:e2e

    
